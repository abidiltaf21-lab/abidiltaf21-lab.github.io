{
  "buildCommand": "cd Frontend/source && npm install && npm run build",
  "outputDirectory": "Frontend/source/dist",
  "installCommand": "echo 'install handled in buildCommand'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}